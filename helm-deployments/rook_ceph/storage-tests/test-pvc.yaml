apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-cephfs-ec-claim
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 6Ti
  storageClassName: rook-ceph-block-storage-ec