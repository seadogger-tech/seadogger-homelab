service:
  enabled: false  # Disable helm chart's service creation

postgresql:
  enabled: true
  primary:
    persistence:
      enabled: false
  auth:
    username: n8n
    password: n8npass
    database: n8n